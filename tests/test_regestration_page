from selenium.webdriver.remote.webdriver import WebDriver
from selenium.webdriver.common.by import By
from main.locators import MainPageLocators


class TestRegestration():

    def test_succesfull_regestration(self, main_page: WebDriver):
        text = main_page.find_element(By.XPATH, MainPageLocators.EXIT_BUTTON).text
        assert "Вход и регистрация" in text
